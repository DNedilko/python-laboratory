import re
import string

def validNames(listOfNames):
    return bool(re.match('\d',listOfNames)
def validScore(ListSCORES):
    return bool(re.match((^[А-Я]{1}[a-я]*\s)*3,ListSCORES))

def ListNames(List):
    list_Names=[]
    for ind in range(0, len(list)-1):
        if list
         ListName=List_Names.append(List[ind])
        else
    return